
sourceSets {
    main {
        java {
            srcDirs = ['src/main/java', ]
        }
        resources {
            srcDirs = ['src/main/resources', ]
        }
    }
}

dependencies {
	compile group: 'com.mitchellbosecke', name: 'pebble', version: '2.4.0'

    compile project(':templatemanagement-domain')
    compile project(':xlapi')
}
